#pragma kernel Clear

RWTexture3D<float4> _VoxelCascade0;
uint resolution;

[numthreads(4,4,4)]
void Clear(uint3 id : SV_DispatchThreadID)
{
	if(any(id >= resolution)) return;

	_VoxelCascade0[id] = float4(0,0,0,0);
}